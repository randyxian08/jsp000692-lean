# JSP-000692 FRO comparator self-check (2026-09-23)

This is submitter-run evidence for **proof commit** [`721bef439aaa4f98b1185c4dc31060f4ae1a3ea0`](https://github.com/randyxian08/jsp000692-lean/tree/721bef439aaa4f98b1185c4dc31060f4ae1a3ea0), not a new proof revision or an organizer decision. The checked target is the fully expanded two-question theorem `JSP692.jsp000692_complete_explicit`. The separately prepared [challenge](Challenge.lean.txt) states the same expanded proposition and imports only the submitted precursor modules `JSP692.AlonGrowth` and `JSP692.LinearIntersection`; it does not import `JSP692.Complete`. The [solution](Solution.lean.txt) supplies the original theorem. Both files were added only to a disposable checkout of the proof commit.

The [FRO comparator](https://github.com/leanprover/comparator/tree/d03acab154d269c06e60e4de7e4cc85deebff94b) was built from commit `d03acab154d269c06e60e4de7e4cc85deebff94b` for Lean 4.34.0. Its pinned [lean4export](https://github.com/leanprover/lean4export/tree/076e8e57707e813375e8f9da8bf989799ace9680) dependency was `076e8e57707e813375e8f9da8bf989799ace9680`; [Landrun](https://github.com/Zouuup/landrun/tree/811cfff51ceaf3d9843708aa6d22e9b84ccac8b4) was built from `811cfff51ceaf3d9843708aa6d22e9b84ccac8b4`. The external nanoda binary was source-built in an earlier local audit from [`3a2407216ee84a75f9e1aead6803d0578be06ae7`](https://github.com/ammkrn/nanoda_lib/tree/3a2407216ee84a75f9e1aead6803d0578be06ae7), with SHA-256 `566a4951761d02fb6bd76a6d8d450b8afdfc0c9796bd183e9ae70c2826dd4038`. The other binary SHA-256 values were: comparator `33e3a03de3c869f76245d27c0ef2a533d8b53ad383f1f4a340268715506ca60d`, lean4export `83807247c0946c6fb699fab7434da4ca44422908dd5301083e0fa262df1ff4bf`, Landrun `dcdfb1d0c106f7ea44bf2d55ae1d2fb1c8cfcb03405d8ef9e216f6235f9d730c`.

The Linux container used the [image recipe](Dockerfile.lean434), image ID `sha256:8e56902d68835f4aca2bfd11a704f0c0211cb3328e68f979458c393e3e76a5c7`, unprivileged uid 10001, four CPUs, a six-GiB memory limit, Lean 4.34.0, and the proof commit's pinned Mathlib `5ed2965256430c3649e86755f9576b54eca72435`. Dependencies and binaries were prepared while network access was enabled. The Docker network was then disconnected; `docker inspect` showed `NetworkSettings.Networks = {}` before both comparator runs. No host credential directories were mounted. Outer `systemd-run` was unavailable in this container; Landrun was invoked by the comparator.

With `Challenge.lean`, `Solution.lean`, [`comparator.json`](comparator.json) and two temporary `[[lean_lib]]` stanzas for them added to a disposable checkout, the following command ran from the proof project root inside that disconnected container (replace `/work` paths if reproducing elsewhere):

```sh
COMPARATOR_LANDRUN=/work/landrun_bin \
COMPARATOR_LEAN4EXPORT=/work/comparator/.lake/packages/lean4export/.lake/build/bin/lean4export \
lake env /work/comparator/.lake/build/bin/comparator comparator.json
```

The [default-kernel log](comparator.log.gz) ends with `Lean default kernel accepts the solution` and `Your solution is okay!`. Its uncompressed SHA-256 is `655dc79af17b2be29743f974e5195a1f86aa57d7902c8e0ae5c720fe41ae967c`. A second run with [`comparator.nanoda.json`](comparator.nanoda.json), `COMPARATOR_NANODA=/work/nanoda_bin`, and the same other environment variables also passed: the [nanoda log](comparator.nanoda.log.gz) records `nanoda kernel accepts the solution` and Lean default-kernel acceptance. Its uncompressed SHA-256 is `995a986429f0fb08f5eea9e0b5357936647bb736866d9a0799e39573e578c22c`. Both configurations permit only `propext`, `Quot.sound`, and `Classical.choice`.

This challenge was authored by the submitter and imports the submitted definitions. The checks support exact Lean-statement replay and restricted axioms; they **do not** independently establish that these definitions express the English Erdős problem, the mathematical originality of the argument, the formalization's priority, verified identity, or prize eligibility. The original PR [#95](https://github.com/TheJustinSunPrize/awards/pull/95) was closed without merge. Its status is unaffected by these self-checks.
